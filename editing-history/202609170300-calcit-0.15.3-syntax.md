# Calcit 0.15.3 语法检查 / Calcit 0.15.3 syntax checks

## 中文

- 将模块、CI 与文档固定到 Calcit 0.15.3。
- 使用 `surface-latest-v2` 检查完整 Snapshot，并应用编译器能够证明安全的语法优化。
- 保持 C-safe Regex 资源、字符串 API 与错误语义不变。

## English

- Pin the module, CI, and documentation to Calcit 0.15.3.
- Check the full Snapshot with `surface-latest-v2` and apply compiler-proven syntax cleanup.
- Preserve the C-safe Regex resource, string API, and error semantics.

## 验证 / Verification

- strict dependency and Calcit type/quality/dynamic-method gates
- public API, examples, definition tests, and Markdown documentation
- Rust tests, strict Clippy, release build, and C-safe export audit
- traced native FFI behavior
