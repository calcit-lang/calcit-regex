# Release 0.0.13

- Publish the merged nominal compiled-regex API, safe compile Result boundary,
  and native regex handle consistency improvements.
- Keep the crate and Calcit module package versions aligned at `0.0.13`.
